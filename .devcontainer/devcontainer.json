{
  "name": "A04 KernelSU Builder",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu-22.04",
  "features": {
    "ghcr.io/devcontainers/features/common-utils:2": {},
    "ghcr.io/devcontainers/features/python:1": {}
  },
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y git make gcc flex bison bc cpio libssl-dev libncurses-dev libelf-dev python3 python3-pip build-essential p7zip-full curl wget xz-utils zstd g++ gawk && sudo apt-get install -y android-sdk-libsparse-utils || true",
  "remoteUser": "codespace",
  "customizations": {
    "vscode": {
      "extensions": ["ms-vscode.cpptools-extension-pack"]
    }
  }
}
